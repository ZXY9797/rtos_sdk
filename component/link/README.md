# 通信 Link 组件边界

`component/link` 是产品无关的通信服务组件，负责帧编解码、路由、ACK、重传
以及链路抽象。

## 允许内容

- 依赖 OSAL 和驱动抽象。
- 通过 `Link` 子类接入 UART、CAN、BLE 等物理链路。
- 通过公开 API 让产品层配置路由表和处理回调。

## 禁止内容

- 不包含具体产品应用头文件。
- 不写死产品地址、产品命令或产品启动策略。
